# docs/

Drop portfolio media here, then uncomment the image tags in the top-level `README.md`:

- **`dashboard.png`** — a screenshot of the dashboard (log in as the demo account for a
  populated view).
- **`demo.gif`** — a short screen recording. The two best moments to capture:
  1. **Live SSE** — reseed/reconnect data and watch the numbers + "● live" indicator update in
     real time as transactions score.
  2. **Connect a bank → "importing…" → data appears** (Plaid sandbox).

Keep files reasonably small (a few MB) so the README loads fast on GitHub.
